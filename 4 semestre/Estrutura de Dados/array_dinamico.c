#include <stdio.h>
#include <stdlib.h>

typedef struct array_dinamico
{
    int *dados;
    int tamanho;
    int capacidade;
}array_dinamico;

array_dinamico* inicializar_array(int capacidade_inicial){

    array_dinamico *array = malloc(sizeof(array_dinamico));
    array - >capacidade = capacidade_inicial;
    array = > 

}